spring:
  application:
    name: userService 
    
  profiles:
    active:
    - dev
    
  cloud:
    config:
      uri: http://localhost:8082

eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/

server:
  port: 8086


  
  