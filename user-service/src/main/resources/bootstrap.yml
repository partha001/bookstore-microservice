spring:
  application:
    name: userService 
    
  profiles:
    active:
    - local
    
  cloud:
    config:
      uri: http://localhost:8082

#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest 

eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/

server:
  port: 8086
