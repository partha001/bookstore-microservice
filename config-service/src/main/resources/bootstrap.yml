spring:
  application:
    name: configService 
    
  profiles:
    active:
    - native  

  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/ 
        
server:
  port: 8082

    
